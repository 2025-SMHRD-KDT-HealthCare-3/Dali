# 1. 핵심 설정 사항
node_modules
dist
dist-ssr
.env
.env.*
.git
*.log

# 2. npm 에러 로그 찌꺼기 방지
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 3. OS 자동 생성 파일 방지
.DS_Store

# 4. 도커 설정 파일은 컨테이너(실행환경) 안에 필요 없음
Dockerfile
.dockerignore
docker-compose.yml