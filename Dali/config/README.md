# config — 설정·상수 보관

이 폴더엔 코드에서 분리한 '바뀔 값'이 들어간다.
모델명·임계 수치·경로 같은 설정을 코드 중간에 박지 않고 여기 모은다.
(CLAUDE.md 분할 기준: "바뀔 값은 코드에서 빼낸다")

예: 모델 설정(gpt-5.4 mini), 위기 감지 임계값(※ 0단계 미정 — 확정 후 추가),
few-shot 경로 등.
※ 로직(코드)은 여기 두지 않는다(pipeline\).
